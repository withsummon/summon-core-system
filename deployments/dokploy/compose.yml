x-app-environment: &app-environment
  WEB_URL: ${WEB_URL:-https://summon-core.withsummon.com}
  DEBUG: "0"
  CORS_ALLOWED_ORIGINS: ${WEB_URL:-https://summon-core.withsummon.com}
  GUNICORN_WORKERS: ${GUNICORN_WORKERS:-2}
  USE_MINIO: "1"
  POSTGRES_USER: ${POSTGRES_USER:-plane}
  POSTGRES_DB: ${POSTGRES_DB:-plane}
  POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
  SECRET_KEY_FILE: /run/secrets/django_secret_key
  SUMMON_CREDENTIAL_KEY_FILE: /run/secrets/summon_credential_key
  REDIS_URL: redis://plane-redis:6379/
  RABBITMQ_USER: ${RABBITMQ_USER:-plane}
  RABBITMQ_VHOST: ${RABBITMQ_VHOST:-plane}
  RABBITMQ_PASSWORD_FILE: /run/secrets/rabbitmq_password
  AWS_REGION: ${AWS_REGION:-}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-summon-core}
  AWS_SECRET_ACCESS_KEY_FILE: /run/secrets/minio_password
  AWS_S3_ENDPOINT_URL: http://plane-minio:9000
  AWS_S3_BUCKET_NAME: ${AWS_S3_BUCKET_NAME:-uploads}
  MINIO_ENDPOINT_SSL: "0"
  API_KEY_RATE_LIMIT: ${API_KEY_RATE_LIMIT:-60/minute}
  LIVE_SERVER_SECRET_KEY_FILE: /run/secrets/live_server_secret_key
  WEBHOOK_ALLOWED_IPS: ${WEBHOOK_ALLOWED_IPS:-}
  WEBHOOK_ALLOWED_HOSTS: ${WEBHOOK_ALLOWED_HOSTS:-}

x-api-service: &api-service
  image: summon-core-api:${SUMMON_IMAGE_TAG:-latest}
  build:
    context: ../../apps/api
    dockerfile: Dockerfile.api
  restart: unless-stopped
  entrypoint: ["./bin/docker-entrypoint-secrets.sh"]
  environment: *app-environment
  secrets:
    - django_secret_key
    - summon_credential_key
    - postgres_password
    - rabbitmq_password
    - minio_password
    - live_server_secret_key
  depends_on:
    plane-db:
      condition: service_healthy
    plane-redis:
      condition: service_healthy
    plane-mq:
      condition: service_healthy

services:
  web:
    image: summon-core-web:${SUMMON_IMAGE_TAG:-latest}
    build:
      context: ../..
      dockerfile: apps/web/Dockerfile.web
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started

  admin:
    image: makeplane/plane-admin:${PLANE_RELEASE:-stable}
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started

  space:
    image: makeplane/plane-space:${PLANE_RELEASE:-stable}
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started

  live:
    image: makeplane/plane-live:${PLANE_RELEASE:-stable}
    restart: unless-stopped
    entrypoint:
      - /bin/sh
      - -ec
      - |
        export LIVE_SERVER_SECRET_KEY="$$(cat /run/secrets/live_server_secret_key)"
        exec docker-entrypoint.sh node apps/live
    environment:
      API_BASE_URL: http://api:8000
      REDIS_URL: redis://plane-redis:6379/
    secrets:
      - live_server_secret_key
    depends_on:
      api:
        condition: service_started
      plane-redis:
        condition: service_healthy

  api:
    <<: *api-service
    command: ./bin/docker-entrypoint-api.sh
    volumes:
      - logs_api:/code/plane/logs

  worker:
    <<: *api-service
    command: ./bin/docker-entrypoint-worker.sh
    volumes:
      - logs_worker:/code/plane/logs
    depends_on:
      api:
        condition: service_started

  beat-worker:
    <<: *api-service
    command: ./bin/docker-entrypoint-beat.sh
    volumes:
      - logs_beat:/code/plane/logs
    depends_on:
      api:
        condition: service_started

  migrator:
    <<: *api-service
    command: ./bin/docker-entrypoint-migrator.sh
    restart: "no"
    volumes:
      - logs_migrator:/code/plane/logs

  plane-db:
    image: postgres:15.7-alpine
    restart: unless-stopped
    command: postgres -c max_connections=1000
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-plane}
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
      POSTGRES_DB: ${POSTGRES_DB:-plane}
    secrets:
      - postgres_password
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-plane} -d ${POSTGRES_DB:-plane}"]
      interval: 10s
      timeout: 5s
      retries: 10

  plane-redis:
    image: valkey/valkey:7.2.11-alpine
    restart: unless-stopped
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  plane-mq:
    image: rabbitmq:3.13.6-management-alpine
    restart: unless-stopped
    entrypoint:
      - /bin/sh
      - -ec
      - |
        export RABBITMQ_DEFAULT_PASS="$$(cat /run/secrets/rabbitmq_password)"
        exec docker-entrypoint.sh rabbitmq-server
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-plane}
      RABBITMQ_DEFAULT_VHOST: ${RABBITMQ_VHOST:-plane}
    secrets:
      - rabbitmq_password
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  plane-minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /export --console-address :9090
    environment:
      MINIO_ROOT_USER: ${AWS_ACCESS_KEY_ID:-summon-core}
      MINIO_ROOT_PASSWORD_FILE: /run/secrets/minio_password
    secrets:
      - minio_password
    volumes:
      - uploads:/export

  proxy:
    image: makeplane/plane-proxy:${PLANE_RELEASE:-stable}
    restart: unless-stopped
    expose:
      - "80"
    environment:
      SITE_ADDRESS: :80
      FILE_SIZE_LIMIT: ${FILE_SIZE_LIMIT:-5242880}
      BUCKET_NAME: ${AWS_S3_BUCKET_NAME:-uploads}
      TRUSTED_PROXIES: 0.0.0.0/0
    depends_on:
      web:
        condition: service_started
      api:
        condition: service_started
      admin:
        condition: service_started
      space:
        condition: service_started
      live:
        condition: service_started

volumes:
  pgdata:
  redisdata:
  rabbitmq_data:
  uploads:
  logs_api:
  logs_worker:
  logs_beat:
  logs_migrator:

secrets:
  django_secret_key:
    file: /etc/dokploy-secrets/summon-core-plane/django_secret_key
  summon_credential_key:
    file: /etc/dokploy-secrets/summon-core-plane/summon_credential_key
  postgres_password:
    file: /etc/dokploy-secrets/summon-core-plane/postgres_password
  rabbitmq_password:
    file: /etc/dokploy-secrets/summon-core-plane/rabbitmq_password
  minio_password:
    file: /etc/dokploy-secrets/summon-core-plane/minio_password
  live_server_secret_key:
    file: /etc/dokploy-secrets/summon-core-plane/live_server_secret_key
